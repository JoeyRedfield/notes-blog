---
tags: [C++, C++ Primer]
---

C++标准并没有规定带符号类型应如何表示，但是约定了在表示范围内正值和负值的量应该平衡。因此，8比特的signed char理论上应该可以表示-127至127区间内的
值，大多数现代计算机将实际的表示范围定为-128至127。

# 2.1.1节练习


练习2.1：类型int、long、long long和short的区别是什么？无符号类型和带符号类型的区别是什么？float和double的区别是什么？

> 都是整型但是表示范围不一样，在64位机器中：2字节short→4字节int → 8字节long → 8字节long long
而在32位机器中，int和long的长度都是4字节，long long是8字节。
无符号类型和带符号类型区别是：无符号类型全部的比特位都用于运算，并且只会是非负数，而带符号类型有一位比特位作为符号位，可以是正数、负数或0。
float和double同为表示浮点数的类型，后者是双精度浮点数，比前者单精度浮点数表示范围要大。
**执行浮点数选用double，因为float通常精度不够且双精度浮点数和单精度浮点数的计算代价相差无几。事实上，对于某些机器来说，双精度运算甚至比单精度还快。**long double提供的精度在一般情况下是没有必要的，况且它带来的运行时消耗也不容忽视。


练习2.2：计算按揭贷款时，对于利率、本金和付款分别应选择何种数据类型？说明你的理由。

> 使用双精度浮点数。因为对于利率、本金和付款必然是有小数存在，且float通常精度不够且双精度浮点数和单精度浮点数的计算代价相差无几，所以使用double类型。

**无符号数和有符号数运算，结果是无符号数**。

经典错误：

```c++
// 无限循环
for (unsigned u = 10; u >= 0; --u) {
		std::cout << u << std::endl;
}
```

# 2.1.2节练习

![[Untitled 86.png]]

> 结果推测：
u2 - u = 32
u - u2 = 10 - 42 = 1(111111….) 10 0000的十进制数：4294967264
i2 - i = 32
i - i2 = -32
i - u = 0
u - i = 0

![[Untitled 87.png]]

字符串字面值的类型实际上是由常量字符构成的数组(aray),该类型将在3.5.4节（第109页)介绍。编译器在每个字符串的结尾处添加一个**空字符（’\0'）**，因此，字符串字面值的实际长度要比它的内容多1。

例如，字面值'A'表示的就是单独的字符A,
而字符串"A"则代表了一个字符的数组，该数组包含两个字符：一个是字母A、另一个是空字符。

# 2.1.3节练习

![[Untitled 88.png]]

> (a). 字符型、宽字符型、字符串型、utf-8字符串。字符/宽字符和字符串/utf-8字符串长度不同，而字符型和字符串型长度有所区别，字符串型实际长度需要把空白符’\0‘算进去，也就是+1.
(b). 整形、无符号整型、long型、无符号long型、整型（八进制）、整型（十六进制），
(c). double型、float型、long double型
(d). 整型、无符号整型、double型、double型


![[Untitled 89.png]]

> 有区别，前者是十进制整型字面值，后者是八进制整型字面值。

![[Untitled 90.png]]

(a). 一个句子，字符串型

(b). long double型字面值，表示3.14 * 10^1，即31.4

(c). float型字面值，表示单精度浮点数，整数部分1024，小数部分0

> 非法，整型字面值不能加后缀f

(d). long double型字面值

练习2.8：请利用转义序列编写一段程序，要求先输出2M,然后转到新一行。修改程序使其先输出2，然后输出制表符，再输出M,最后转到新一行。

```c++

void practice2_8() {
	std::cout << "2M\n";
	std::cout << "2\t\m\n" << std::endl;
}
```

**初始化不是赋值**，初始化的含义是创建变量时赋予其一个初始值，而赋值的含义是把对象的当前值擦除，而以一个新值来替代。

一种未被初始化的内置类型变量的值是**未定义**的，如果试图拷贝或以其他形式访问此类值将引发错误。

# 2.2.1节练习

![[Untitled 91.png]]

(a). 输入流写入input_value，而int是非法定义，应去掉

(b). 非法，花括号定义对于可能会丢失精度的定义会报非法，应使用(d)定义，虽然会丢数据，

(c). 相当于把未定义的wage的值赋给salary，是非法的，应该分开定义。

![[Untitled 92.png]]

> global_str: “”，空字符串
global_int: 0

main内：local_int: 未初始化， local_str: 未初始化

为了支持**分离式编译**，C++语言将**声明（declaration）和定义（definition）**区分开来。

![[Untitled 93.png]]

# 2.2.2节练习

![[Untitled 94.png]]

定义、定义、声明

# 2.2.3节练习

![[Untitled 95.png]]

(a)(c)(d)非法，(b)(e)合法

# 2.2.4节练习

![[Untitled 96.png]]

2.13：100

2.14：合法，输出i=10，sum=45


引用即别名：引用并非对象，相反的，它只是为一个已经存在的对象所起的另一个名字。

引用本身不是一个对象，所以不能定义引用的引用。

除了两种例外情况（书第55页、534页），其他所有引用类型都要和与之绑定的对象严格匹配。而且，**引用只能绑定在对象上**，而不能与字面值或某个表达式的计算结果绑在一起。

# 2.3.1节练习

![[Untitled 97.png]]

练习2.15：(b)不合法是因为引用只能绑定在对象上，(d)是因为引用必须初始化

练习2.16：
(a)合法，前面r2已经进行了初始化，然后将r2改为指向了3.14159对象，
(b)合法，即使引用的类型没有严格匹配（int和double），但那是初始化时需要注意的地方，和(d)同理。
(c)合法，将r2所指向的d的值赋给了i，截断了小数部分,
(d)合法

![[Untitled 98.png]]

![[Untitled 99.png]]

10 10

### 2.3.2 指针

指针(pointer)是“指向(point to)”另外一种类型的复合类型。
与引用类似，指针也实现了对其他对象的间接访问。然而指针与引用相比又有很多不同点。
其一，指针本身就是一个对象，允许对指针赋值和拷贝，而且在指针的生命周期内它可以先后指向几个不同的对象。
其二，指针无须在定义时赋初值。和其他内置类型一样，在块作用域内定义的指针如果没有被初始化，也将拥有一个不确定的值。

除了两种例外情况（书第56页、534页），其他所有指针类型都要和它所指向的对象严格匹配。
因为在声明语句中指针的类型实际上被用于指定它所指向对象的类型，所以二者必须匹配。如果指针指向了一个其他类型的对象，对该对象的操作将发生错误。
要跟使用引用的情况区分开来（练习2.16）

![[Untitled 100.png]]

当用到一个预处理变量时，预处理器会自动地将它替换为实际值，因此**用NULL初始化指针和用0初始化指针是一样的**。在新标准下，现在的C++程序最好使用nullptr，同时尽量避免使用NULL。

```c++
int *pi = 0; // pi被初始化，但没有指向任何对象

int i = 0;
int *j = i; // 把int变量直接赋给指针是错误的操作，即使int变量的值恰好等于0也不行。

pi = &ival; //这是修改了指针指向的对象，也就是存放在pi内的地址，修改为ival
*pi = 0; // ival的值被改变，指针pi存放的地址并没有改变
```

### 2.3.2节练习

练习2.18：编写代码分别更改指针的值以及指针所指对象的值。

```c++
// 练习2.18：编写代码分别更改指针的值以及指针所指对象的值。
void practice2_18() {
	int i = 2, * p = 0; // p被初始化，不指向任何对象
	p = &i; // 修改p指针所存地址为i的地址
	std::cout << "i = " << i << ", *p = " << *p << ", p = " << p << std::endl;
	*p = 666; // 修改p指针所指向的对象的值，也就是i
	std::cout << "i = " << i << ", *p = " << *p << ", p = " << p << std::endl;
}
```

![[Untitled 101.png]]

练习2.19：说明指针和引用的主要区别

主要区别是前者是对象，后者不是，后者相当于对象的别名。
引用本身不是一个对象，所以不能定义引用的引用，但可以定义指针的指针。

练习2.20：请叙述下面这段代码的作用；

```c++
void practice2_20() {
	int i = 42;
	int* p1 = &i;
	*p1 = *p1 * *p1;
}
```

用指针去求42的平方，同时将i修改为其结果。

练习2.21：请解释下述定义。在这些定义中有非法的吗？如果有，为什么？

```c++
int i = 0;
(a) double* dp = &i; (b) int* ip = i; (c) int *p = &i;
```

(a)非法，因为类型不一致，(b)非法，不能把int整型变量赋值给指针对象。

(c)合法

练习2.22：假设p是一个int型指针，请说明下述代码的含义。

```c++
if(p) // ...
if(*p) // ...
```

第一个if表示如果p指针存放的地址本身不为空，则为true，否则为false；
第二个if表示如果p指针指向的地址，其存放的对象的int值不为0，则为true，否则为false；


练习2.23：给定指针p，你能知道它是否指向了一个合法的对象吗？如果能，叙述判断
的思路；如果不能，也请说明原因。

我觉得不能，p指向一个未知的对象，如果做操作的话会比较危险

练习2.24：在下面这段代码中为什么p合法而lp非法？


```c++
int i=42;
void *p = &i;
long *lp = &i;
```

因为void*是空类型指针，可以~~存放~~接受任意类型的对象，所以p合法，而lp是long型指针，&i是int型变量i的地址，int和long类型不匹配，所以非法。

```c++
int* p1, p2, *p; // p1是指向int的指针，p2是int

// r是对指针p的引用，从右往左阅读r的定义：离变量名最近的符号（&）对变量的
// 类型有最直接的影响，因此r是一个引用。其余部分（*）说明r引用的是一个指针。
// 最后，声明的基本数据类型部分指出r引用的是一个int指针
int *&r = p; 

```

### 2.3.3节练习

![[Untitled 102.png]]

（a）ip：int型指针，i：int型变量，r：int引用，是i的别名，

（b）i：int型变量，ip：int型空指针。

（c）ip：int型指针，ip2：int型变量

# 2.4 const限定符

默认情况下，`const`对象被设定为仅在文件内有效。当多个文件中出现了同名的`const`变量时，其实等同于在不同文件中分别定义了独立的变量。
也可以只在一个文件中定义`const`，而在其他多个文件中声明并使用它。这时候需要对于`const`变量不管是声明还是定义都添加`extern`关键字，

![[Untitled 103.png]]

## 2.4节练习

![[Untitled 104.png]]

(a)不合法，没有初始化，
(d)不合法，const修饰的变量不能改本其本身的值，题中是++sz不合法。

## 2.4.1 const的引用

与普通引用不同的是，对常量的引用不能被用作修改它所绑定的对象。也就是只能const引用对const常量。

![[Untitled 105.png]]

### 初始化对const的引用

引用的类型必须与其所引用的对象的类型一致，但对`const`的引用是例外的

```c++
double dval = 3.14;
const int &ri = dval;

// 编译器实际形式：
const int temp = dval; // 临时的变量
const int &ri = temp;

// 显然，如果ri不是常量，就允许对ri赋值，也就会改变ri所引用对象的值，
// 但是这里temp实际绑定的是一个临时量，改不掉dval的值。实际上这是非法行为
```

特殊情况，对const的引用可能引用一个并非`const`的对象

```c++
int i = 42;
const int& r1 = i;
i = 43;
std::cout << r1 << std::endl; // 43
```

## 2.4.1 指针和const

要想存放常量对象的值，只能使用指向常量的指针。

指针的类型必须与其所指对象的类型一致，但有两个例外：
1. 允许令一个指向常量的指针指向一个非常量对象（对比上面的常量引用）

有指向常量的指针（`pointer to const`），也有常量指针（`const pointer`），后者类比其它常量类型，必须初始化且初始化完成后，存放在指针中的那个地址就不能再改变了。

`*const`从右往左读，说明是常量指针。

## 2.4.2节练习

练习2.27：下面的哪些初始化是合法的？请说明原因。

![[Untitled 106.png]]

假设i2是一个已经初始化的int型变量/常量

(a).非法，非常量引用r不能引用字面值常量0。

(b).合法

(c).合法，相对于(a)，i是一个常量，r是一个常量引用，此时r可以绑定到字面值常量0.

(d).合法

(e).合法

(f).不合法，没有初始化

(g).合法

练习2.28：说明下面的这些定义是什么意思，挑出其中不合法的。

![[Untitled 107.png]]

(a).声明了int型变量i，int型常量指针cp，后者不合法因为没有被初始化。

(b).声明了int型指针p1，int型常量指针p2，同上。

(c).声明了int型常量ic，int型常量引用r指向ic。不合法，因为常量必须被初始化。

(d).声明了int型常量指针p3，不合法，因为常量指针必须初始化

(e).声明了int型指针p，指向int型的常量。这个是合法的（经过测试），因为这不是常量指针。

> 注意`pointer to const`和`const pointer`的区别

练习2.29：假设已有上一个练习中定义的那些变量，则下面的哪些语句是合法的？请说明原因。

![[Untitled 108.png]]

不太理解这种”已有“的意思，假设是已经初始化。

(a).合法，把常量值赋给变量是允许的。

(b).是非法的，普通指针 p1 指向了一个常量，从语法上说，p1 的值可以随意改变，显然是不合理的。因为 p1 改变了，p1 所指对象因该也同时改变了，但是 p1 所指对象是一个常量，不能被修改。

(c).是非法的，普通指针 p1 指向了一个常量，错误情况与上一条类似。

(d).不合法

(e).不合法

(f).不合法

### 2.4.3 顶层const

![[Untitled 109.png]]

**练习2.30：**

```plain text
顶层 const 表示任意的对象是常量，而底层 const 与指针和引用等复合类型的基本类型部分有关。

解答：v2 和 p3 是顶层 const，分别表示一个整型常量和一个整型常量指针；p2 和 r2 是底层 const，分别表示它们所指（所引用）的对象是常量。
```

**练习2.31：**


```plain text
本题考查顶层 const 和底层 const 对于拷贝操作的影响。

解答：

在执行拷贝操作时，顶层 const 和底层 const 区别明显。其中，顶层 const 不受影响，这是因为拷贝操作并不会改变被拷贝对象的值。底层 const 的限制则不容忽视，拷入和拷出的对象必须具有相同的底层 const 资格，或者两个对象的数据类型必须能够转换。一般说来，非常量可以转换成常量，反之则不行。

r1 = v2; 是合法的，r1 是一个非常量引用，v2 是一个常量（顶层 const），把 v2 的值拷贝给 r1 不会对 v2 有任何影响。

p1 = p2; 是非法的，p1 是普通指针，指向的对象可以是任意值，p2 是指向常量的指针（底层 const），令 p1 指向 p2 所指的内容，有可能错误地通过 p1 改变 p2 所指常量的值。

p2 = p1; 是合法的，与上一条语句相反，p2 可以指向一个非常量，只不过我们不会通过 p2 更改它所指的值。

p1 = p3; 是非法的，p3 包含底层 const 定义（p3 所指的对象是常量），不能把 p3 的值赋给普通指针。

p2 = p3; 是合法的，p2 和 p3 包含相同的底层 const，p3 的顶层 const 则可以忽略不计。
```

### 2.4.4 constexpr和常量表达式

![[Untitled 110.png]]

显然不合法，int没办法给int *赋初值。给后面的null加&就行。至少在vs2022能正常退出。

![[Untitled 111.png]]

# 2.5 处理类型

讲的typedef、auto、decltype关键词。

不要把typedef错误地理解成只是把名称替换。

## 2.5.2节练习

![[Untitled 112.png]]

**2.33:** 根据vs2022判断, 
a: a的类型是int型, 可以赋值42;
b: b类型是int型, 顶层const被忽略掉了, 可以赋值42;
c: c类型是int &型, 和b同理; 
d: 是int *型, 不能用int类初始化;
e: 是const int*, 且顶层const被忽略掉;
g: 是const int& , 且顶层const被忽略掉

2.34: 同上. 

2.35: j是int型, k是const int&, p是const int*型, j2是const int型; k2是const int&型.

decltype((var))注意是双层括号时的结果永远是引用, 而decltype(var)的结果只有当var本身就是一个引用时才是引用. 

## 2.5.3节练习

![[Untitled 113.png]]

2.36: c: int型, d: int &型, c = 4, a = d = 4, b = 4

2.37: a = 3, b = 4, c: int型 = 3, d: int&型 = 3; 注意表达式并没有给真的给a赋值

![[Untitled 114.png]]

2.38: auto指定类型是根据右边的值来决定的, 而decltype是根据括号内变量类型或表达式的返回值来决定的. 这种特性决定了auto必然会有初始化的值, 而decltype则不一定. 
decltype指定类型与auto指定类型一样的情况: decltype(a) i; auto i = a; 
不一样的情况则是decltype((a)), 此时返回int&类型.

> auto 和 decltype 的区别主要有三方面：

auto 类型说明符用编译器计算变量的初始值来推断其类型，而 decltype 虽然也让编译器分析表达式并得到它的类型，但是不实际计算表达式的值。
编译器推断出来的 auto 类型有时候和初始值的类型并不完全一样，编译器会适当地改变结果类型使其更符合初始化规则。例如，auto 一般会忽略掉顶层 const，而把底层 const 保留下来。与之相反，decltype 会保留变量的顶层 const。
与 auto 不同，decltype 的结果类型与表达式密切相关，如果变量名加上了一对括号，则得到的类型与不加括号时会有不同。如果 decltype 使用的是一个不加括号的变量，则得到的结果就是该变量的类型；如果给变量加上了一层或多层括号，则编译器将推断得到引用类型。

## 2.6.1节练习

![[Untitled 115.png]]

2.39提示”没加分号”,

2.40不写, 做下面的题也是一样的. 

## 2.6.2节练习

![[Untitled 116.png]]

由于没写头文件, 操作符也没有重载, 所以实际上是把之前写过的内容, 改成用结构体内的一个一个数据类型操作.

## 2.6.3 编写自己的头文件/练习

头文件一旦改变, 相关的源文件必须重新编译以获取更新过的声明. 

新关键字:

#define, #ifdef, #ifndef, #endif. 

实际上没教操作符重载之类的内容, 写完了就算引入, 也还是一个一个数据类型地操作.

# 小结

类型是C++编程的基础。
类型规定了其对象的存储要求和所能执行的操作。C++语言提供了一套基础内置类型，如int和cha等，这些类型与实现它们的机器硬件密切相关。类型分为非常量和常量，一个常量对象必须初始化，而且一旦初始化其值就不能再改变。此外，还可以定义复合类型，如指针和引用等。复合类型的定义以其他类型为基础。
C++语言允许用户以类的形式自定义类型。C++库通过类提供了一套高级抽象类型，如输入输出和string等。
