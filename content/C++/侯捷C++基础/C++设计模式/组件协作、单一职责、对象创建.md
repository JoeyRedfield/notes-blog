---

---
# “组件协作“模式

现代软件专业分工之后的第一个结果是“框架与应用程序的划分”，“组件协作”模式通过晚期绑定，来实现框架与应用程序之间的松耦合，是二者之间协作时常用的模式。

## Template Method

### 动机

在软件构建过程中，对于某一项任务，它常常有**稳定**的整体操作结构，但各个子步骤却有很多**改变**的需求，或者由于固有的原因（比如框架与应用之间的关系）而无法和任务的整体结构同时实现。

如何在确定稳定操作结构的前提下，来灵活应对各个子步骤的变化或者晚期实现需求？

### 结构化软件设计流程

![[Untitled 872.png]]

### 面向对象软件设计流程

![[Untitled 873.png]]

### 早绑定与晚绑定

![[Untitled 874.png]]

### 模式定义

> 定义一个操作中的算法的骨架(稳定)，而将一些步骤**延迟(变化)**到子类中。Template Method使得子类可以不改变(复用)一个算法的结构即可重定义(override 重写)该算法的某些特定步骤。
——《设计模式》GOF

### 结构（Structure）

![[Untitled 875.png]]

TemplateMethod()是稳定的, Primitive…是变化的. 

![[Untitled 876.png]]

MainTemplate设置了2个虚函数step2()和step3(), 具体实现留给Application1和Application2, 而业务逻辑放在父类的run方法中. 这里Application2必须要写step3()的具体实现, 不然会被判断为抽象类. 

```plain text
step 1
step 4
step 4
-------------
step 1
step 4
step 4
step 4
step 4
```

### 要点总结

- Template Method模式是一种非常基础性的设计模式，在面向对象系统中有着大量的应用。它用**最简洁的机制（虚函数的多态性）**为很多应用程序框架提供了灵活的**扩展点**，是代码复用方面的基本实现结构。
- 除了可以灵活应对子步骤的变化外，“不要调用我，让我来调用你”的**反向控制结构**是Template Method的典型应用。
- 在具体实现方面，被Template Method调用的虚方法可以具有实现，也可以没有任何实现（抽象方法、纯虚方法），但一般**推荐将它们设置为protected方法**。

## Strategy

### 动机

- 在软件构建过程中，某些对象使用的算法可能多种多样，经常改变，如果将这些算法都编码到对象中，将会使对象变得异常复杂；而且有时候支持不使用的算法也是一个性能负担。
- 如何在运行时根据需要透明地更改对象的算法？将算法与对象本身解耦，从而避免上述问题？

### 模式定义

> 定义一系列算法，把它们一个个封装起来，并且使它们可互相替换（变化）。该模式使得算法可独立于使用它的客户程序(稳定)而变化（扩展，子类化）。——《设计模式》GOF

### 结构

![[Untitled 877.png]]

应用场景有很多, 比如各个国家不同的税的计算, 洗衣机模式的选择等.

```c++
#include <iostream>
#include <string>
using namespace std;

// 策略模式的抽象策略类，定义了运行模式的接口run()
class IMode
{
public:
	virtual void run() = 0;
	virtual ~IMode() {};
};

// 两个具体策略类
class Dry : public IMode
{
public:
	virtual ~Dry() { cout << "remove dry" << endl; }
	virtual void run() { cout << "dry mode" << endl; }
};

class Wash : public IMode
{
public:
	virtual ~Wash() { cout << "remove Wash" << endl; }
	virtual void run() { cout << "Wash mode" << endl; }
};

// 策略模式的上下文类，其中包含了 IMode* mode 成员变量，
// 代表了当前使用的模式。在 start() 方法中调用了当前模式的run()方法。
class Laundry
{
public:
	IMode* mode;
	Laundry(IMode* m) : mode(m) {}
	void start() {
		mode->run();
	}
	void setMode(IMode* m) {
		mode = m;
	}
};

// main.cpp
#include "IStrategy.h"
int main() {
	IMode* d = new Dry();
	IMode* w = new Wash();
	Laundry l(d);
	char c;
	while (cin >> c) {
		switch (c)
		{
		case 'd' :
			l.setMode(d); l.start(); break;
		case 'w':
			l.setMode(w); l.start(); break;
		default:
			break;
		}
		break;
	}
	delete d;
	delete w;
}
```

### 要点总结

- Strategy及其子类为组件提供了一系列可重用的算法，从而可以使得类型在**运行时**方便地根据需要在各个算法之间进行切换。
- Strategy模式提供了用条件判断语句以外的另一种选择，**消除条件判断语句**，就是在解耦合。含有许多条件判断语句的代码通常都需要Strategy模式。
- 如果Strategy对象没有实例变量，那么各个上下文可以共享同一个Strategy对象，从而节省对象开销。

## Observer / Event

### 动机

- 在软件构建过程中，我们需要为某些对象建立一种“通知依赖关系” ——一个对象（目标对象）的状态发生改变，所有的依赖对象（观察者对象）都将得到通知。如果这样的依赖关系过于紧密，将使软件不能很好地抵御变化。
- 使用面向对象技术，可以将这种依赖关系弱化，并形成一种稳定的依赖关系。从而实现软件体系结构的松耦合。

### 模式定义

> 定义对象间的一种一对多（变化）的依赖关系，以便当一个对象(Subject)的状态发生改变时，所有依赖于它的对象都得到通知并自动更新。——《设计模式》GoF

### 结构

![[Untitled 878.png]]

### 案例

![[Untitled 879.png]]

```c++
#include "IObserver.h"

int main() {
    Subject* subject = new Subject;
    Observer* observer1 = new Observer(*subject); // Hi, I'm the Observer "1".
    Observer* observer2 = new Observer(*subject); // Hi, I'm the Observer "2".
    Observer* observer3 = new Observer(*subject); // Hi, I'm the Observer "3".
    Observer* observer4;
    Observer* observer5;

    // There are 3 observers in the list.
    // Observer "1": a new message is available --> Hello World! :D
    // Observer "2": a new message is available-- > Hello World! :D
    // Observer "3": a new message is available-- > Hello World!:D
    subject->CreateMessage("Hello World! :D");

    observer3->RemoveMeFromTheList(); // Observer "3" removed from the list.

    // There are 2 observers in the list.
    // Observer "1": a new message is available-- > The weather is hot today!:p
    // Observer "2": a new message is available-- > The weather is hot today!:p
    subject->CreateMessage("The weather is hot today! :p");

    observer4 = new Observer(*subject); // Hi, I'm the Observer "4".

    observer2->RemoveMeFromTheList(); // Observer "2" removed from the list.
    observer5 = new Observer(*subject); // Hi, I'm the Observer "5".

    // There are 3 observers in the list.
    // Observer "1": a new message is available-- > My new car is great!;)
    // Observer "4": a new message is available-- > My new car is great!;)
    // Observer "5": a new message is available-- > My new car is great!;)
    subject->CreateMessage("My new car is great! ;)");

    observer5->RemoveMeFromTheList(); // Observer "5" removed from the list.
    observer4->RemoveMeFromTheList(); // Observer "4" removed from the list.
    observer1->RemoveMeFromTheList(); // Observer "1" removed from the list.

    // Goodbye, I was the Observer "...".
    delete observer5;
    delete observer4;
    delete observer3;
    delete observer2;
    delete observer1;
    delete subject;
}
```

### 要点总结

- 使用面向对象的抽象，Observer模式使得我们可以独立地改变目标与观察者，从而使二者之间的依赖关系达致松耦合。
- 目标发送通知时，无需指定观察者，通知（可以携带通知信息作为参数）会自动传播。
- 观察者自己决定是否需要订阅通知，目标对象对此一无所知。
- Observer模式是基于事件的UI框架中非常常用的设计模式，也是MVC模式的一个重要组成部分。

# 单一职责

在软件组件的设计中，如果责任划分得不清晰，使用继承得到的结果往往是随着需求的变化，子类急剧膨胀，同时充斥着重复代码，这时候的关键是划清责任。

## Decorator

### 动机

- 在某些情况下我们可能会“过度地使用继承来扩展对象的功能”由于继承为类型引入的静态特质，使得这种扩展方式缺乏灵活性；并且随着子类的增多（扩展功能的增多），各种子类的组合（扩展功能的组合）会导致更多子类的膨胀。
- 如何使“对象功能的扩展”能够根据需要来动态地实现？同时避免“扩展功能的增多”带来的子类膨胀问题？从而使得任何“功能扩展变化”所导致的影响将为最低？

### 模式定义

> 动态（组合）地给一个对象增加一些额外的职责。就增加功能而言，Decorator模式比生成子类（继承）更为灵活（消除重复代码 & 减少子类个数）。——《设计模式》GoF

### 结构

![[Untitled 880.png]]

### 案例

设计IO库，Stream基类有Read，Seek，Write方法，但是有文件加密类CryptoFileStream, CryptoNetworkStream流做**额外的(但相同且重复)加密操作**，这类流的Read等操作需要用到别的流的Read操作 (比如FileStream, NetworkStream)再做加密. 

![[Untitled 881.png]]

```c++
// 抽象组件（Component）类
class Stream
{
public:
	virtual void read(const string& s) = 0;
	virtual void seek(const int position) = 0;
	virtual void write(const string& s) = 0;
};

// 具体组件（Concrete Component）类
class FileStream : public Stream {
public:
	virtual void read(const string& s) {
		cout << "FileStream read: " << s << endl;
	}
	virtual void seek(const int position) {
		cout << "FileStream position: " << position << endl;
	}
	virtual void write(const string& s) {
		cout << "FileStream write: " << s << endl;
	}
};

// 具体组件（Concrete Component）类
class MemoryStream : public Stream {
public:
	virtual void read(const string& s) {
		cout << "MemoryStream read: " << s << endl;
	}
	virtual void seek(const int position) {
		cout << "MemoryStream position: " << position << endl;
	}
	virtual void write(const string& s) {
		cout << "MemoryStream write: " << s << endl;
	}
};
```

```c++
// 抽象装饰器（Decorator）类
class DecoratorStream : public Stream
{
protected:
	Stream* stream;
	DecoratorStream(Stream* stm):stream(stm){}
};

// 具体装饰器（Concrete Decorator）类
// 使用了stream等同于去找了Conctete Component的Operation()
class CryptoStream : public DecoratorStream
{
public:
	CryptoStream(Stream* stm) : DecoratorStream(stm) {}
	virtual void read(const string& s) {
		cout << "CryptoStream read: " << s << endl;
		stream->read(s); 
	}
	virtual void seek(const int position) {
		cout << "CryptoStream position: " << position << endl;
		stream->seek(position);
	}
	virtual void write(const string& s) {
		cout << "CryptoStream write: " << s << endl;
		stream->write(s);
	}
};
```

```c++
int main() {
    Stream* fileStream = new FileStream();
    Stream* memoryStream = new MemoryStream();

    // 不加密的操作
    fileStream->read("file data");
    memoryStream->write("memory data");

    // 使用加密装饰器来加密数据流
    // 这里是运行时再决定ConcreteComponent
    Stream* encryptedFileStream = new CryptoStream(fileStream);
    Stream* encryptedMemoryStream = new CryptoStream(memoryStream);

    // 加密后的操作
    encryptedFileStream->read("encrypted file data");
    encryptedMemoryStream->write("encrypted memory data");

    // 释放资源
    delete encryptedFileStream;
    delete encryptedMemoryStream;
    delete fileStream;
    delete memoryStream;

    return 0;
}
```

### 要点总结

- 通过采用组合而非继承的手法， Decorator模式实现了在运行时 动态扩展对象功能的能力，而且可以根据需要扩展多个功能。避免 了使用继承带来的“灵活性差”和“多子类衍生问题”
- Decorator类在接口上表现为is-a Component的继承关系，即 Decorator类继承了Component类所具有的接口。但在实现上又 表现为has-a Component的组合关系，即Decorator类又使用了 另外一个Component类。
- Decorator模式的目的并非解决“多子类衍生的多继承”问题， Decorator模式应用的要点在于解决“主体类在多个方向上的扩展 功能”——是为“装饰”的含义。

## Bridge

### 动机

- 由于某些类型的固有的实现逻辑，使得它们具有两个变化的维度，乃至多个纬度的变化。
- 如何应对这种“多维度的变化”？如何利用面向对象技术来使得类型可以轻松地沿着两个乃至多个方向变化，而不引入额外的复杂度？

### 模式定义

> 将抽象部分(业务功能)与实现部分(平台实现)分离，使它们都可以独立地变化。——《设计模式》GoF

### 结构

![[Untitled 882.png]]

### 应用场景

老师给的案例是Messager用户类和MessagerImp平台类的两个变化的维度。比如说Messager可以登录，登录之后要借助平台的MessagerImp的Connect，而有多个平台比如PC、Mobile。而Messager也有Lite和Perfect（轻量级和重量级）的区别。

感觉不如之前课本上的红蓝形状组合。

![[Untitled 883.png]]

```c++
class Platform
{
public:
	virtual void playSound() = 0;
	virtual void backgroundImg() = 0;
	virtual void vip() = 0;
};

class PlatformLite : public Platform {
public:
	virtual void playSound() {
		cout << "lite playSound" << endl;
	}
	virtual void backgroundImg() {
		cout << "lite bgimg" << endl;
	}
	virtual void vip() {
		cout << "lite, not vip" << endl;
	}
};

class PlatformVIP : public Platform {
public:
	virtual void playSound() {
		cout << "VIP playSound" << endl;
	}
	virtual void backgroundImg() {
		cout << "VIP bgimg" << endl;
	}
	virtual void vip() {
		cout << "VIP vip" << endl;
	}
};
```

```c++
class Messager
{
protected:
	Platform* p;
public:
	Messager(Platform* pf) :p(pf) {};
	virtual void login() = 0;
	virtual void sendMsg() = 0;
	virtual void sendImg() = 0;
};

class PCMessager : public Messager
{
public:
	PCMessager(Platform* pf) : Messager(pf) {}
	virtual void login() {
		p->playSound();
		cout << "PC login" << endl;
	}
	virtual void sendMsg() {
		cout << "PC sendMsg" << endl;
	}
	virtual void sendImg() {
		p->vip();
		cout << "PC sendImg" << endl;
	}
};

class MobileMessager : public Messager
{
public:
	MobileMessager(Platform* pf) : Messager(pf) {}
	virtual void login() {
		p->playSound();
		cout << "MobileMessager login" << endl;
	}
	virtual void sendMsg() {
		cout << "MobileMessager sendMsg" << endl;
	}
	virtual void sendImg() {
		p->vip();
		cout << "MobileMessager sendImg" << endl;
	}
};
```

```c++
int main() {

    Platform* platformLite = new PlatformLite();
    Platform* platformVIP = new PlatformVIP();

		Messager* pcMessager = new PCMessager(platformLite); // 用户使用PC端Lite平台
    Messager* mobileMessager = new MobileMessager(platformVIP); // 用户使用移动端VIP平台

    // lite playSound
    // PC login
    pcMessager->login();

    pcMessager->sendMsg(); // PC sendMsg

    // lite, not vip
    // PC sendImg
    pcMessager->sendImg();

    // VIP playSound
    // MobileMessager login
    mobileMessager->login();
    mobileMessager->sendMsg(); // MobileMessager sendMsg

    // VIP vip
    // MobileMessager sendImg
    mobileMessager->sendImg();

    delete pcMessager;
    delete mobileMessager;
    delete platformLite;
    delete platformVIP;

    return 0;
}
```

### 要点总结

- Bridge模式使用“对象间的组合关系”解耦了抽象和实现之间固 有的绑定关系，使得抽象和实现可以沿着各自的维度来变化。所谓抽象和实现沿着各自纬度的变化，即“子类化”它们。
- Bridge模式有时候类似于多继承方案，但是多继承方案往往违背 单一职责原则（即一个类只有一个变化的原因），复用性比较差。Bridge模式是比多继承方案更好的解决方法。
- Bridge模式的应用一般在“两个非常强的变化维度”，有时一个 类也有多于两个的变化维度，这时可以使用Bridge的扩展模式。

# 对象创建

通过“对象创建” 模式**绕开new**，来避免对象创建（new）过程中所导致的紧耦合（依赖具体类），从而支持对象创建的稳定。它是接口抽象之后的第一步工作。

## Factory Method（深入理解）

### 动机

- 在软件系统中，经常面临着创建对象的工作；由于需求的变化，需要创建的对象的具体类型经常变化。
- 如何应对这种变化？如何绕过常规的对象创建方法(new)，提供一种“封装机制”来避免客户程序和这种“具体对象创建工作”的紧耦合？

### 模式定义

> 定义一个用于创建对象的接口，让子类决定实例化哪一个类。Factory Method使得一个类的实例化延迟（目的：解耦，手段：虚函数）到子类。——《设计模式》GoF

### 结构

![[Untitled 884.png]]

### 应用场景

MainForm下有文件切割器Splitter，分割器有很多种，它们的实例化需要大量的操作，所以要避开new，做到解耦合。

![[Untitled 885.png]]

```c++
class ISplitter
{
public:
	virtual void split() = 0;
	virtual ~ISplitter() {};
};

class BinarySpliter : public ISplitter {
public:
	virtual void split() {
		cout << "binary" << endl;
	}
};
```

```c++
class SplitterFactory
{
public:
	virtual ISplitter* createSplitter() = 0;
	virtual ~SplitterFactory() {}
};

class BinarySpliterFactory : public SplitterFactory {
public:
	virtual ISplitter* createSplitter() {
		return new BinarySpliter();
	}
};

class MainForm
{
	SplitterFactory* sf;
	ISplitter* is;
public:
	MainForm(SplitterFactory* s): sf(s), is(sf->createSplitter()){
		
	}
	void click() {
		is->split();
	}
	virtual ~MainForm() {};
};
```

```c++
int main() {
    // 创建一个BinarySpliterFactory
    SplitterFactory* factory = new BinarySpliterFactory();

    // 使用BinarySpliterFactory创建MainForm
    MainForm* form = new MainForm(factory);

    // 点击按钮，会使用BinarySpliterFactory创建BinarySpliter，并调用split()函数
    form->click();

    // 可以在需要的时候更换工厂，从而创建不同的产品
    // 这里换成其他工厂，比如换成其他类型的工厂，如TextSpliterFactory
    // factory = new TextSpliterFactory();

    // 点击按钮，会使用TextSpliterFactory创建TextSpliter，并调用split()函数
    // form->click();

    // 释放资源
    delete form;
    delete factory;

    return 0;
}
```

### 要点总结

- Factory Method模式用于隔离类对象的使用者和具体类型之间的耦合关系。面对一个经常变化的具体类型，紧耦合关系(new)会导致软件的脆弱。
- Factory Method模式通过面向对象的手法，将所要创建的具体对象工作延迟到子类，从而实现一种扩展（而非更改）的策略，较好地解决了这种紧耦合关系。
- Factory Method模式解决“单个对象”的需求变化。缺点在于要求创建方法/参数相同。

## Abstract Factory

### 动机

- 在软件系统中，经常面临着“**一系列相互依赖的对象**”的创建工作；同时，由于需求的变化，往往存在更多系列对象的创建工作。
- 如何应对这种变化？如何绕过常规的对象创建方法(new)，提供一种“封装机制”来避免客户程序和这种“多系列具体对象创建工作”的紧耦合？

### 模式定义

> 提供一个接口，让该接口负责创建**一系列“相关或者相互依赖的对象”**，无需指定它们具体的类。——《设计模式》GoF

### 结构

![[Untitled 886.png]]

### 应用场景

IDBFactory类内可以创建Connection、Command、DataReader对象，各个对象都要支持SQL Server、Oracle等厂商。

![[Untitled 887.png]]

图中, IDBFactory是AbstractFactory, 它有2个ConcreteFactory分别是SQLDB和Oracle, 它们各自都能CreateProduct. 而IDBConnection, IDBCommand, IDBDataReader属于AbstractProduct, 两个厂商创建的是具体的ProductA2, A1, B2, B1, 代码中体现的是SQLDBConnection, OracleConnection等.

```c++
class IDBConnection
{
public:
	virtual void dbconn() = 0;
	virtual ~IDBConnection() {}
};

class IDBCommand
{
public:
	virtual void dbcomm() = 0;
	virtual ~IDBCommand() {}
};

class IDBDataReader
{
public:
	virtual void dataReader() = 0;
	virtual ~IDBDataReader() {}
};

class IDBFactory
{
public:
	virtual IDBConnection* createConnection() = 0;
	virtual IDBCommand* createCommand() = 0;
	virtual IDBDataReader* createDataReader() = 0;
	virtual ~IDBFactory() {}
};
```

```c++
class SQLDBConnection : public IDBConnection
{
public:
	virtual void dbconn() {
		cout << "sqldb dbconn" << endl;
	}
};

class SQLDBCommand : public IDBCommand
{
public:
	virtual void dbcomm() {
		cout << "sqldb dbcomm" << endl;
	}
};

class SQLDBDataReader: public IDBDataReader
{
public:
	virtual void dataReader() {
		cout << "sqldb dbcomm" << endl;
	}
};

class SQLFactory : public IDBFactory
{
public:
	virtual IDBConnection* createConnection() {
		return new SQLDBConnection();
	}
	virtual IDBCommand* createCommand() {
		return new SQLDBCommand();
	}
	virtual IDBDataReader* createDataReader() {
		return new SQLDBDataReader();
	}
};
```

```c++
class OracleConnection : public IDBConnection
{
public:
	virtual void dbconn() {
		cout << "Oracle dbconn" << endl;
	}
};

class OracleCommand : public IDBCommand
{
public:
	virtual void dbcomm() {
		cout << "Oracle dbcomm" << endl;
	}
};

class OracleDataReader : public IDBDataReader
{
public:
	virtual void dataReader() {
		cout << "Oracle dbcomm" << endl;
	}
};

class OracleFactory : public IDBFactory
{
public:
	virtual IDBConnection* createConnection() {
		return new OracleConnection();
	}
	virtual IDBCommand* createCommand() {
		return new OracleCommand();
	}
	virtual IDBDataReader* createDataReader() {
		return new OracleDataReader();
	}
};
```

```c++
int main() {
    IDBFactory* factory = new SQLFactory();

    IDBConnection* connection = factory->createConnection();
    IDBCommand* command = factory->createCommand();
    IDBDataReader* dataReader = factory->createDataReader();

    connection->dbconn();
    command->dbcomm();
    dataReader->dataReader();

    delete connection;
    delete command;
    delete dataReader;
    delete factory;

    factory = new OracleFactory();

    connection = factory->createConnection();
    command = factory->createCommand();
    dataReader = factory->createDataReader();

    connection->dbconn();
    command->dbcomm();
    dataReader->dataReader();

    delete connection;
    delete command;
    delete dataReader;
    delete factory;

}
```

### 要点总结

- 如果没有应对“多系列对象构建”的需求变化，则没有必要使用Abstract Factory模式，这时候使用简单的工厂完全可以。
- “系列对象”指的是在某一特定系列下的对象之间有相互依赖、或作用的关系。不同系列的对象之间不能相互依赖。
- Abstract Factory模式主要在于应对“**新系列**”的需求变动。其缺点在于难以应对“新对象”的需求变动。

## Prototype

这个用得比较少，讲的是clone(), 通过克隆自己来创建对象

### 动机

- 在软件系统中，经常面临着“**某些结构复杂的对象**”的创建工作；由于需求的变化，这些对象经常面临着剧烈的变化，但是它们却拥有比较稳定一致的接口。
- 如何应对这种变化？如何向“客户程序（使用这些对象的程序）”隔离出“这些易变对象”，从而使得“依赖这些易变对象的客户程序”不随着需求改变而改变？

### 模式定义

> 使用原型实例指定创建对象的种类，然后通过拷贝这些原型来创建新的对象。
——《设计模式》GoF

### 结构

![[Untitled 888.png]]

### 应用场景

以工厂模式为例, C++的体现是这样的:

```c++
// 不再由工厂生成, 让Splitter自己构造(深拷贝)自己, 得到新对象
class BinarySpliter : public ISplitter {
public:
	virtual ISplitter* clone() {
		return new BinarySplitter(*this);
	}
};

// 还要给BinarySplitter类补充一个拷贝构造函数
// 拷贝构造函数，进行深度复制
BinarySplitter(const BinarySplitter& other) {
    // 根据情况复制资源或指针
    // 比如，如果BinarySplitter类包含指针成员，则需要进行深度复制
}
```

在这个函数中，`**this**`是一个指向当前对象的指针，而`***this**`则表示当前对象本身。

1. 传入*this：使用`**return new BinarySplitter(*this);**`，将当前对象本身作为参数传递给`**BinarySplitter**`类的拷贝构造函数，从而创建一个新的`**BinarySplitter**`对象，该对象与当前对象具有相同的状态。这样的实现方式典型地符合原型模式，通过复制（克隆）现有对象来创建新对象。
2. 不传入*this：如果不使用`**this**`，而是直接`**return new BinarySplitter();**`，则会调用`**BinarySplitter**`类的默认构造函数来创建一个新的`**BinarySplitter**`对象，但是新对象的状态和当前对象的状态无关，它将是一个全新的对象。

总结：传入`***this**`表示在克隆过程中**使用当前对象本身作为原型来创建新对象**，而不传入`***this**`则表示在克隆过程中创建一个全新的对象。原型模式通常需要传入`***this**`来实现对象的克隆。

### 要点总结

- Prototype模式同样用于隔离类对象的使用者和具体类型(易变类)之间的耦合关系，它同样要求这些“易变类”拥有“**稳定的接口**”。
- Prototype模式对于“如何创建易变类的实体对象”采用“原型克隆”的方法来做，它使得我们可以非常灵活地动态创建“拥有某些稳定接口”的新对象一一所需工作仅仅是注册一个新类的对象（即原型）然后在任何需要的地方Clone。
- Prototype模式中的Clone方法可以利用某些框架中的序列化来实现深拷贝。

## Builder

这个也用得比较少

### 动机

- 在软件系统中，有时候面临着“**一个复杂对象**”的创建工作，其通常由各个部分的子对象用一定的算法构成；由于需求的变化，**这个复杂对象的各个部分经常面临着剧烈的变化，但是将它们组合在一起的算法却相对稳定**。
- 如何应对这种变化？如何提供一种“封装机制”来隔离出“复杂对象的各个部分”的变化，从而保持系统中的“稳定构建算法”不随着需求改变而改变？

### 模式定义

> 将一个复杂对象的构建与其表示相分离，使得同样的构建过程(稳定)可以创建不同的表示(变化)。——《设计模式》GoF

### 结构

![[Untitled 889.png]]

### 应用场景

首先要知道C++中, 父类构造函数是没办法调用**子类**虚函数的. 简单来说是因为子类创建前需要先生成父类(调用父类构造函数), 而代码中构造父类又需要调用子类虚函数的话, dddd. [之前](/30854062eb1f46a5aa41fae34714542c#38c63944ed184d63a40609dc1982372c)有讲过. 

视频讲的是建房子, House, HouseBuilder, HouseDirector.  House作为Product交给Builder来生成, 管理. 在该场景中建房子的(算法)流程是稳定的, 所以抽取出来做成Construct()给Director. 

![[Untitled 890.png]]

```c++
using namespace std;

class House
{
public:
	virtual ~House() {}
};

class StoneHouse : public House
{
public:

};

class HouseBuilder
{
protected:
	House* pHouse;
public:
	House* getResult() {
		return pHouse;
	}
	virtual void buildPart1() = 0;
	virtual void buildPart2() = 0;
	virtual void buildPart3() = 0;
	virtual ~HouseBuilder() {}
};

class StoneHouseBuilder : public HouseBuilder
{
public:
	StoneHouseBuilder() {
		pHouse = new StoneHouse();
	}
	virtual void buildPart1() {
		cout << "StoneHouse part1" << endl;
	};
	virtual void buildPart2() {
		cout << "StoneHouse part2" << endl;
	};
	virtual void buildPart3() {
		cout << "StoneHouse part3" << endl;
	};
};


class HouseDirector
{
public:
	HouseBuilder* pHouseBuilder;
	HouseDirector(HouseBuilder* hb) : pHouseBuilder(hb) {}
	House* Construct() {
		pHouseBuilder->buildPart1();
		pHouseBuilder->buildPart2();
		pHouseBuilder->buildPart3();
		return pHouseBuilder->getResult();
	}
};
```

```c++
int main() {
    HouseBuilder* stoneHouseBuilder = new StoneHouseBuilder();
    HouseDirector director(stoneHouseBuilder);
    House* stoneHouse = director.Construct();

    // Use the stoneHouse object as needed.

    delete stoneHouse;
    delete stoneHouseBuilder;
    return 0;
}
```

### 要点总结

- Builder 模式主要用于“分步骤构建一个复杂的对象”。在这其中“分步骤”是一个稳定的算法(Construct)，而复杂对象的各个部分则经常变化。
- 变化点在哪里，封装哪里—— Builder模式主要在于应对“复杂对象各个部分”的频繁需求变动。其缺点在于难以应对“分步骤构建算法”的需求变动。
- 在Builder模式中，要注意不同语言中构造器内调用虚函数的差别（C++ vs. C#) 。
